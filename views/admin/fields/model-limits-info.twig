{# Current Model Limits Card #}
<div class="viscribe-card viscribe-model-limits-card">
    <div class="viscribe-card-header">
        <h2>
            <span class="dashicons dashicons-performance"></span>
            {{ model_limit_info.title }}
            <a href="{{ model_limit_info.model_card_url }}" target="_blank" rel="noopener noreferrer" class="viscribe-doc-badge">
                <span class="dashicons dashicons-book"></span>
                <span>{% trans %}Model Card{% endtrans %}</span>
            </a>
        </h2>
        <p class="viscribe-card-description">{{ model_limit_info.description }}</p>
    </div>

    <div class="viscribe-card-body">
        <p class="viscribe-model-limit-meta">
            <strong>{{ model_limit_info.model_label }}</strong>
            <code class="viscribe-model-limit-code">{{ model_limit_info.model_id }}</code>
        </p>

        <div class="viscribe-model-limit-list">
            {% for item in model_limit_info.items %}
                <article class="viscribe-model-limit-item">
                    <div class="viscribe-model-limit-item-header">
                        <h3>{{ item.label }}</h3>
                        <span class="viscribe-model-limit-value">{{ item.value }}</span>
                    </div>
                    <p class="viscribe-model-limit-meaning">{{ item.meaning }}</p>
                </article>
            {% endfor %}
        </div>

        <div class="viscribe-model-limit-notes">
            {% for note in model_limit_info.notes %}
                <div class="viscribe-model-limit-note viscribe-model-limit-note--{{ note.variant }}">
                    <span class="dashicons dashicons-info-outline"></span>
                    <div class="viscribe-model-limit-note-body">
                        <strong>{{ note.title }}</strong>
                        <p>{{ note.text }}</p>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</div>

